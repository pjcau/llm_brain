[package]
name = "sync-diagrams"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Copies diagrams/*.mmd into the ```mermaid blocks marked {/* diagram: NAME */} in docs/**/*.md"

[dependencies]
